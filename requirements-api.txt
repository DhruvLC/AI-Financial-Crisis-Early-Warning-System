# Minimal runtime deps for the Backend Deployment API image.
# (Training-only deps — torch, shap, kaggle, matplotlib — are intentionally
# excluded; the API only serves the persisted sklearn best model.)
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
pyyaml>=6.0
joblib>=1.3
pyarrow>=14.0
xgboost>=2.0
# pipeline.ml package imports its visualization module at import time;
# matplotlib/seaborn are therefore needed to load the registry (viz unused).
matplotlib>=3.7
seaborn>=0.12
fastapi>=0.110
uvicorn[standard]>=0.29
