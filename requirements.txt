# Core
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
pyyaml>=6.0

# Data ingestion
kaggle>=1.6
requests>=2.31
pyarrow>=14.0        # parquet interim storage
yfinance>=0.2        # Yahoo Finance source
fredapi>=0.5         # optional; FRED works via CSV fallback without it

# Models
xgboost>=2.0

# Explainability
shap>=0.44

# Viz (EDA / reports)
matplotlib>=3.7
seaborn>=0.12

# --- Phase 2 (uncomment when you build the deep-learning stages) ---
# torch>=2.1
# transformers>=4.40
# yfinance>=0.2      # market data (stage 2)
# fredapi>=0.5       # macro data (stage 2)
